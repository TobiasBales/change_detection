<%= form_with(model: change_detector) do |form| %>
  <% if change_detector.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(change_detector.errors.count, "error") %> prohibited this change_detector from being saved:</h2>

      <ul>
        <% change_detector.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
